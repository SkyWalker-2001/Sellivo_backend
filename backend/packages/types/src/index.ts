/**
 * @sellivo/types — shared TypeScript types across the backend API and Next.js web.
 *
 * Populated in later Phase 0 steps (roles, tenancy scope, sync DTOs). Kept as an
 * explicit placeholder so the workspace graph is coherent from the start.
 */

export type Role = "owner" | "manager" | "cashier";

export {};
